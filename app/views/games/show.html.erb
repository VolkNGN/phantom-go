<div class="game-show-container">
  <!-- Header -->
  <%= render "games/header" %>

  <!-- Opponent -->
  <%= render "games/opponent" %>

  <!-- Goban (plateau de jeu) -->
  <%= render "games/goban" %>

  <!-- Current Player -->
  <%= render "games/current_player" %>

  <!-- referee -->
  <%= render "games/referee" %>

  <!-- button game -->
  <%= render "games/button_game" %>



</div>

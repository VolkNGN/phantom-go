<div class="button-container d-flex justify-content-center gap-3 mt-3">
  <!-- Bouton Abandonner -->
  <%= link_to "Quitter", root_path, class: "btn btn-danger btn-abandon d-flex justify-content-between align-items-center" %>
  <%= link_to "Passer", "#{@game.id}/pass", class: "btn btn-primary btn-pass d-flex justify-content-between align-items-center", data: { turbo_method: :patch } %>

</div>
